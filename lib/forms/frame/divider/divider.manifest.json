{
  "$schema": "../../schema/frame.schema.json",
  "id": "divider",
  "form": "divider",
  "kind": "sovereign",
  "exemptFromChrome": true,
  "description": "Section divider bookend — its own centred chrome claims the canvas, so the root chrome Cells do not apply (design/forms.md §8; FORM_TOGGLE_SKIP). A section-boundary slide.",
  "cells": ["stage"],
  "suppresses": ["masthead", "masthead-lede", "masthead-bay", "footer", "footer-left", "progress-centre", "pagination-right"]
}
