{
  "$schema": "../../schema/frame.schema.json",
  "id": "minimal",
  "form": "bookend",
  "kind": "root",
  "exemptFromChrome": false,
  "description": "The quieter root chrome Frame: masthead band (lede + bay), stage, and footer — but the progress rail is suppressed (the `form: minimal` toggle adds `no-progress`). For a less busy deck (design/forms.md §9).",
  "cells": [
    "masthead",
    "masthead-lede",
    "masthead-bay",
    "stage",
    "footer",
    "footer-left",
    "pagination-right",
    "overlay"
  ],
  "suppresses": ["progress-centre"]
}
