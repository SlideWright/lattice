{
  "$schema": "../../schema/frame.schema.json",
  "id": "math",
  "form": "stack",
  "kind": "sovereign",
  "exemptFromChrome": true,
  "description": "Typeset-equation Frame — drives its own `> h2` title grid, so the root masthead does not apply (design/forms.md §8; FORM_TOGGLE_SKIP). Suppresses the masthead; keeps the footer.",
  "cells": ["stage"],
  "suppresses": ["masthead", "masthead-lede", "masthead-bay"]
}
