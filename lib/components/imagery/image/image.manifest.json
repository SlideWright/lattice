{
  "$schema": "../../manifest.schema.json",
  "name": "image",
  "function": "imagery",
  "form": "canvas",
  "substance": "prose",
  "orientation": ["landscape", "portrait"],
  "tags": ["visual", "showcase", "pitch"],
  "description": "Image as the slide's anchor, with optional text alongside — composition adapts to the asset and the deck.",
  "purpose": "Use when a visual carries meaning on its own. You hand it any rectangle; the layout reads the asset's aspect at build time and, with the deck orientation, RESOLVES the composition for you — no modifier needed. The default is `clean` (a floated card shaped to the photo, ≈ zero crop); extreme aspects auto-pick `split` (shown whole) or `spotlight` (full-bleed cover). Name a composition to override: `clean` · `split` · `spotlight` · `gallery` (contain-on-matte, for diagrams) · `statement` (full-bleed + scrim + title). `mirror` flips the image side. Legacy `full`/`contain`/`museum` still work (→ spotlight/gallery/gallery).",
  "variants": [
    "clean",
    "split",
    "spotlight",
    "gallery",
    "statement",
    "mirror"
  ],
  "slots": {
    "image": {
      "selector": ".lattice-bg",
      "required": true,
      "description": "Marp background image syntax: `![bg](path)` or `![bg right](path)` — rendered as a CSS background-image on the `.lattice-bg` panel (no `<img>`)."
    },
    "heading": {
      "selector": "h2",
      "required": false,
      "description": "Optional heading in the text slot."
    },
    "body": {
      "selector": "p",
      "required": false,
      "description": "Optional caption or body text."
    }
  },
  "skeleton": "<!-- _class: image -->\n\n## Text leads; the image earns its place.\n\nReplace the bg image below with your own asset — any aspect. The layout reads its shape and resolves the composition for you (a floated card, a full-height column, a full-bleed cover). Name a composition (`image spotlight`, `image gallery`, …) only to override.\n\n![bg](your-image.jpg)\n",
  "anatomyBlock": "image-canvas",
  "sample": "<!-- _class: image -->\n\n## Activation is where the trial is won or lost.\n\nTwo-thirds of trials that reach the first generated report convert to paid; the ones that stall at workspace setup almost never do. Hand the layout any photo — it resolves the composition from the asset's shape.\n\n![bg](sample-photo-wide.svg)\n",
  "whenToUse": [
    {
      "title": "The visual carries meaning",
      "body": "Product screenshots, architectural photographs, plots, satellite imagery — anywhere the image makes the argument and the prose is annotation. If the visual is decorative, drop it and use `content` instead."
    },
    {
      "title": "Let the layout resolve it — override only with intent",
      "body": "Drop in any aspect and the layout reads it: a moderate photo gets the `clean` floated card, a tall/wide one gets `split` (shown whole), a canvas-matching one gets `spotlight` (full-bleed). Override only when you mean it: `image gallery` to contain a diagram zero-crop, `image statement` for a scrim-and-title hero, `image spotlight` to force a full-bleed cover (accepting the crop)."
    },
    {
      "title": "Caption earns its line",
      "body": "If the prose alongside the image just describes what the image shows, drop it — the audience can see the picture. The text slot is for the so-what: what the audience should take away from the visual."
    }
  ],
  "antiPatterns": [
    {
      "title": "Decorative stock photo",
      "body": "A generic photograph of 'people in a meeting' next to a content slide is filler. Use `content` and trust the prose; reserve image for visuals that argue for themselves."
    },
    {
      "title": "Image too small to read",
      "body": "A diagram or screenshot small enough to fit inside a half-canvas text slot is unreadable from the back of the room. Reach for `image gallery` (contains it whole) or `image spotlight`, or move the diagram to its own `diagram` slide."
    },
    {
      "title": "Image with five paragraphs of caption",
      "body": "If the prose dominates and the image is a sidebar, you have a `content` slide that happens to have a photo. Either trust the image (drop the prose) or trust the prose (drop the image)."
    }
  ],
  "related": [
    {
      "name": "diagram",
      "when": "the visual is a Mermaid graph, not a photo or screenshot"
    },
    {
      "name": "content",
      "when": "the slide is mostly prose with one inline visual"
    },
    {
      "name": "title",
      "when": "the image is a bookend hero, not the body of a slide"
    },
    {
      "name": "quote",
      "when": "the slide is a quotation, not an image"
    }
  ],
  "variantDocs": {
    "clean": {
      "label": "Clean — the default floated card (auto)",
      "caption": "The standing default for a moderate-aspect photo. A floated card whose ASPECT adapts to the asset (a square photo → square card, a wide photo → wide card), beside a text panel (landscape) or stacked above it (portrait). Cover-fill of a card shaped like the photo means ≈ zero crop. Auto-resolves for `square` and `wide` assets; name it to force a card on any asset.",
      "sample": "<!-- _class: image clean -->\n\n## Activation is where the trial is won or lost.\n\nTwo-thirds of trials that reach the first generated report convert; the ones that stall almost never do.\n\n![bg](sample-photo-wide.svg)\n"
    },
    "split": {
      "label": "Split — an extreme-aspect photo, shown whole (auto)",
      "caption": "For a `tall`/`column` asset on a landscape deck (a full-height image column + text) or a `wide`/`pano` asset on a portrait deck (a full-width image band + text below). Shows the whole photo with ≈ zero crop and fills the canvas a moderate card would leave empty.",
      "sample": "<!-- _class: image split -->\n\n## Built for the long climb.\n\nA portrait photo wants its full height. We give it a column and let the argument run alongside.\n\n![bg](sample-photo-tall.svg)\n"
    },
    "spotlight": {
      "label": "Spotlight — full-bleed cover + a solid card (auto)",
      "caption": "For an asset whose aspect already MATCHES the canvas (a `pano` on landscape, a `tall` on portrait): the photo goes full-bleed and the text rides a SOLID card, so legibility never depends on the photo. Name it to force a full-bleed cover on any asset (accepting the crop).",
      "sample": "<!-- _class: image spotlight -->\n\n## A panorama earns the full frame.\n\nWhen the photo already matches the canvas, let it carry the slide — the message rides in a solid card so it never fights the image.\n\n![bg](sample-photo-pano.svg)\n"
    },
    "gallery": {
      "label": "Gallery — contain on a matte + placard (opt-in)",
      "caption": "Opt-in. Contains the WHOLE asset on a matte with a centered placard caption — zero crop, letterboxed. For diagrams, screenshots, and plots where the whitespace is meaningful and every pixel matters. Never auto-resolved (we can't detect a diagram from aspect alone), so name it.",
      "sample": "<!-- _class: image gallery -->\n\n## Exhibit 1 — the network, contained.\n\nThe whole asset on a matte with a placard. For diagrams and screenshots where the whitespace is the point.\n\n![bg](sample-photo-square.svg)\n"
    },
    "statement": {
      "label": "Statement — full-bleed + scrim + editorial title (opt-in)",
      "caption": "Opt-in. The title rides the photo on a diagonal scrim — a deliberate, editorial moment for an opener or closer. White text over an unknown photo is a legibility gamble, so it is never auto-resolved; reach for it when you know the photo carries it.",
      "sample": "<!-- _class: image statement -->\n\n## The setup step is the real funnel.\n\nThe title rides the photo on a scrim — a deliberate, editorial choice.\n\n![bg](sample-photo-wide.svg)\n"
    },
    "mirror": {
      "label": "Mirror — flip the image to the other side",
      "caption": "Flips the image/text side for the `clean` and `split` compositions (image left, text right). Equivalent to `![bg left]`. Use when the surrounding spread reads right-to-left or when the page-turn cue lands on the image side.",
      "sample": "<!-- _class: image mirror -->\n\n## Mirror lands the image on the left.\n\nText leads from the right; image anchors from the left.\n\n![bg left](sample-photo-wide.svg)\n"
    }
  },
  "showcase": { "featured": true }
}
