{
  "$schema": "../../manifest.schema.json",
  "name": "compare-table",
  "adapt": { "mode": "single-orientation", "priority": ["title", "table"], "keepTogether": ["row"] },
  "focusAxes": ["row", "col", "cell"],
  "capacity": { "axis": "row", "min": 2, "sweet": 4, "soft": 6, "hard": 8, "escalateTo": ["split across slides"], "note": "the table density crowds past six rows" },
  "split": { "strategy": "cover-cards", "axis": "row", "bodyAxis": "item", "perPage": 2, "intro": "Row by row", "coverClass": "compare-table", "docs": "With `autosplit: on`, a `compare-table` too wide or too tall for the frame (most often a portrait deck) reflows **card-per-row**: an accent cover, then each row as a card — its first cell the heading, every other column a `label: value` pair carrying the `<thead>` label down with the value. You read each row top-to-bottom instead of squinting across columns, and the run is paginated and counted with a k-of-N rail. Authoring is unchanged — a normal Markdown table; the split only fires when it overflows.", "note": "An overflowing compare-table (autosplit: on) splits as an accent cover → card-per-row: each row becomes a card (first cell = heading; every other column = a label:value pair from the <thead>), so a wide table reflows into a NARROW/portrait frame and paginates DOWN the tall frame — row pagination alone can't fix the horizontal overflow a portrait box creates. Card pages re-split on the item axis (bodyAxis). The cover carries compare-table's design language via coverClass. lib/core/carousel.js cover-cards; cover-cards narrows the body so the measure gate treats it as width-fixable (widthFix=true)." },
  "function": "comparison",
  "form": "ledger",
  "substance": "prose",
  "orientation": ["landscape"],
  "tags": ["tradeoff", "ranking", "assessment"],
  "description": "Multi-row comparison table with consistent columns.",
  "purpose": "Use when you have 3+ options or 4+ rows of criteria. Wider data than compare-prose can hold legibly.",
  "slots": {
    "title": {
      "selector": "h2",
      "required": true,
      "description": "Slide heading framing the comparison."
    },
    "table": {
      "selector": "table",
      "required": true,
      "description": "Markdown table with header row and 2+ data rows."
    }
  },
  "skeleton": "<!-- _class: compare-table -->\n\n## Heading framing the comparison.\n\n| Criterion | Option A | Option B | Option C |\n| --- | --- | --- | --- |\n| First criterion | Value | Value | Value |\n| Second criterion | Value | Value | Value |\n| Third criterion | Value | Value | Value |\n",
  "anatomyBlock": "T22-compare-table",
  "sample": "<!-- _class: compare-table -->\n\n## How the three planning rituals compare.\n\n| Ritual | Cadence | Output | Read after the meeting |\n| --- | --- | --- | --- |\n| Strategy offsite | Yearly | An 80-slide deck | The title slide |\n| Quarterly business review | Quarterly | A scorecard | The red cells |\n| Weekly signal review | Weekly | A decision-log entry | Whoever wrote it |\n",
  "whenToUse": [
    {
      "title": "Wider than compare-prose",
      "body": "Three or more options, or four or more rows of criteria. compare-prose maxes out at two columns and short bodies; compare-table scales further."
    },
    {
      "title": "Cells are short phrases",
      "body": "Each cell is a value, a phrase, or a state marker — not a paragraph. If the cells need sentences, use `verdict-grid` or `cards-stack`."
    },
    {
      "title": "Stable column meaning",
      "body": "Every row reads the same way across columns. Mixing column meanings row-to-row breaks the table's scannability."
    }
  ],
  "antiPatterns": [
    {
      "title": "Cells full of prose",
      "body": "Long sentences in a table cell wrap awkwardly and force the column wider. Move to `verdict-grid` for criteria with body text, or `cards-stack` for full prose rows."
    },
    {
      "title": "More than 6 rows",
      "body": "Past 6 rows the table density crowds the slide. Summarise the rows that don't differentiate, split into two slides — or turn on `autosplit: on` and let it reflow card-per-row (see Overflow)."
    },
    {
      "title": "State-marker rows",
      "body": "When most cells are pass/fail/partial badges, the right layout is `obligation-matrix` or `verdict-grid`. compare-table is for textual values."
    }
  ],
  "related": [
    { "name": "compare-prose", "when": "exactly two options with prose bodies" },
    { "name": "verdict-grid", "when": "options scored against criteria with pass/partial/fail badges" },
    { "name": "obligation-matrix", "when": "many regimes compared against shared obligations" },
    { "name": "cards-stack", "when": "each row needs full-prose breathing room rather than a tabular cell" }
  ]
}
