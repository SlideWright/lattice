{
  "$schema": "../../manifest.schema.json",
  "name": "timeline",
  "function": "progression",
  "form": "timeline",
  "substance": "structure",
  "tags": ["milestones", "planning", "changelog"],
  "description": "Horizontal ordered steps along a single axis, each a labeled dot.",
  "purpose": "Use for sequential processes with 3–6 stages. Ordered list (ol) renders numbered circles; unordered (ul) renders plain dots.",
  "slots": {
    "title": {
      "selector": "h2",
      "required": true,
      "description": "Slide heading naming the process."
    },
    "steps": {
      "selector": "ol > li, ul > li",
      "required": true,
      "description": "One li per step. The lead text is the step label — it renders bold automatically (no `**…**` needed); follow it with a nested bullet for the description."
    }
  },
  "skeleton": "<!-- _class: timeline -->\n\n## How the process flows.\n\n1. First stage\n   - *Short description of what happens here.*\n2. Second stage\n   - *Short description.*\n3. Third stage\n   - *Short description.*\n4. Fourth stage\n   - *Short description.*\n",
  "anatomyBlock": "timeline",
  "sample": "<!-- _class: timeline -->\n\n## From first pilot to general availability.\n\n1. Pilot\n   - *Four product teams run the framework for a quarter against a shared baseline.*\n2. Calibrate\n   - *Scoring weights are tuned against real outcomes; the decision log becomes mandatory.*\n3. Roll out\n   - *Every product team onboards and the weekly signal review joins the operating rhythm.*\n4. GA\n   - *The framework leaves pilot status and ships as the default for new initiatives.*\n",
  "whenToUse": [
    {
      "title": "Short labels on a horizontal axis",
      "body": "When each stage is a short label plus a sentence — the audience reads the whole sequence as a single arc. Richer per-step bodies belong in `list-steps`."
    },
    {
      "title": "Three to six stages",
      "body": "Below three the axis feels under-furnished; above six the dots crowd and the labels collide. Group adjacent stages or split into two slides."
    },
    {
      "title": "Ordered or unordered",
      "body": "Use `ol` for numbered circles (sequence is the point); use `ul` for plain dots (the stages are a journey rather than a ranked sequence)."
    }
  ],
  "antiPatterns": [
    {
      "title": "Long body per step",
      "body": "If each stage needs a paragraph, lift to `list-steps` where the cards have body room. Prose crammed into a timeline label crowds the axis."
    },
    {
      "title": "Parallel options, not sequence",
      "body": "If the items are alternatives rather than ordered stages, use `cards-grid`. The horizontal axis here reads as time."
    },
    {
      "title": "Past six stages",
      "body": "More than six dots compresses the axis and the labels collide. Split into two slides or roll up adjacent stages."
    }
  ],
  "related": [
    {
      "name": "list-steps",
      "when": "each step needs body-paragraph room, not just a label"
    },
    {
      "name": "split-steps",
      "when": "a phase anchor + heading reads on the left, steps on the right"
    },
    {
      "name": "journey",
      "when": "each step carries actors and mood, not just a label"
    },
    {
      "name": "gantt",
      "when": "tasks have duration and overlap across multiple workstreams"
    },
    {
      "name": "roadmap",
      "when": "phased grid across multiple workstreams rather than a single axis"
    }
  ]
}
