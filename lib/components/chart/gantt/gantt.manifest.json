{
  "$schema": "../../manifest.schema.json",
  "name": "gantt",
  "adapt": { "mode": "reflow" },
  "function": "progression",
  "bucket": "chart",
  "form": "timeline",
  "substance": "series",
  "orientation": ["landscape", "portrait"],
  "tags": ["swimlane", "planning", "milestones", "agile"],
  "description": "Gantt chart — task bars across a date axis.",
  "purpose": "Use for project plans with overlapping or staggered tasks. Each task is a bar on the time axis; bars can span multiple periods and carry status tints.",
  "slots": {
    "title": {
      "selector": "h2",
      "required": true,
      "description": "Slide heading naming the plan."
    },
    "tasks": {
      "selector": "ul > li",
      "required": true,
      "description": "Outer li per workstream lane; nested bullets per task. Each task carries trailing inline-code tokens, in any order: a span `START..END` (a bar) or a single time point (a milestone diamond); an optional status; an optional `after: Task name` dependency; an optional `milestone` keyword. `..` is the only span delimiter. Time points are ISO dates (2026-03-15), quarters (Q1 or 2026 Q1), or months (Jan); a chart uses dates OR ordinals, not both. Status vocabulary: on-track / done / live / at-risk / warn / blocked / fail / deferred / pilot / decision. The axis derives from the data; the eyebrow may override it with a `START..END` window and add a `today <point>` marker. Tokens are validated by the linter (retired delimiter, bad span/status, dangling or inverted `after:`)."
    },
    "detail": {
      "selector": "ul > li > ul > li > ul > li",
      "required": false,
      "description": "Optional per-task reveal detail. A nested bullet under a TASK (one level deeper than the task) — plain prose: the owner, the blocker, the why — is captured as that task's detail rather than rendered on the bar. It drives two surfaces from one source: (1) on screen (Drawing Board present/practice/preview) the bar/milestone is tagged data-mark and the prose rides an inert `<template class=\"chart-detail\">` the reveal layer shows in a popover on hover/tap, with the active bar lifted + glowing and the rest dimmed (gantt is reveal-only — no 3D tilt, which would skew the time axis); (2) the static PDF — the same detail folds into the slide's speaker note (`Task (span): item · item`) as a Marp-faithful comment. Renders nothing on the chart face, so a chart with no detail bullets is byte-identical. Must be a bullet, not a trailing inline-code token."
    }
  },
  "skeleton": "<!-- _class: gantt -->\n\n`2026 Q1 .. 2026 Q4` `today Q3`\n\n## What ships in each phase, by workstream.\n\n- First workstream\n  - First task `Q1..Q2` `done`\n  - Second task `Q2..Q3` `live` `after: First task`\n  - Milestone `Q4` `milestone` `after: Second task`\n- Second workstream\n  - First task `Q1..Q2` `done`\n  - Second task `Q2..Q3`\n",
  "anatomyBlock": "gantt",
  "sample": "<!-- _class: gantt -->\n\n`2026 Q1 .. 2026 Q4` `today Q3`\n\n## What ships in each phase, by workstream.\n\nThree workstreams across four quarters; the one at-risk bar quietly gates the rollout, GA is a milestone, and the today line marks where the plan stands.\n\n- Framework\n  - Signal taxonomy `Q1..Q2` `done`\n  - Scoring model v2 `Q2..Q3` `live` `after: Signal taxonomy`\n  - Per-team weighting `Q3..Q4` `at-risk` `after: Scoring model v2`\n    - Two teams contest the weighting; the Q3 review decides it, and the rollout waits on the outcome.\n- Adoption\n  - Pilot onboarding `Q1..Q2` `done`\n  - Org-wide rollout `Q3..Q4` `after: Per-team weighting`\n  - GA `Q4` `milestone` `after: Org-wide rollout`\n    - Go/no-go gate: needs SOC2 sign-off and the weighting decision landed.\n",
  "whenToUse": [
    {
      "title": "Overlapping work across lanes",
      "body": "When tasks run in parallel across multiple workstreams and the audience needs to see who is busy when. The lane-stacked bars make concurrency visible at a glance."
    },
    {
      "title": "Span is the story",
      "body": "Each bar's length encodes its duration. Use gantt when start dates, end dates, and overlap are what you want the audience to remember."
    },
    {
      "title": "Status pills add a second channel",
      "body": "Tint bars with `done` / `live` / `at-risk` / `blocked` to layer health onto schedule. The plan reads as both 'when' and 'how it's going' in one chart. Since the bars carry no status text, a swatch+label status key is emitted automatically below the chart for the statuses present."
    }
  ],
  "antiPatterns": [
    {
      "title": "Single workstream",
      "body": "One lane of bars is a timeline, not a gantt. Use `timeline` or `list-steps` when there is no parallel work to coordinate."
    },
    {
      "title": "More than five lanes",
      "body": "Past five workstreams the bars compress and the labels crowd. Group lanes (collapse 'SDK' subdomains into 'SDK') or split into two slides."
    },
    {
      "title": "No spans at all",
      "body": "A gantt mixes bars with the odd milestone — but if every task is a point-in-time event with no durations, use `timeline` or `roadmap milestones`. gantt earns its chrome only when bars carry meaningful length."
    }
  ],
  "related": [
    {
      "name": "roadmap",
      "when": "phased grid of deliverables across workstreams without continuous spans"
    },
    
    {
      "name": "kanban",
      "when": "current state by stage rather than schedule by lane"
    },
    {
      "name": "list-steps",
      "when": "sequential process with descriptive steps, no parallel lanes"
    }
  ],
  "stressSample": "<!-- _class: gantt -->\n\n`2026 Q1 .. 2026 Q4`\n\n## Stress test — four workstreams, twelve tasks, one recalibration bar nobody wants to own.\n\n- Signal Intake\n  - Connector v1 `Q1..Q2` `done`\n  - Multi-source dedupe `Q2..Q3` `live`\n  - Anomaly auto-routing `Q3..Q4` `at-risk`\n- Scoring\n  - Equal-weights model `Q1..Q2` `done`\n  - Per-team calibration `Q2..Q3` `live`\n  - Weight rollback tooling `Q3..Q4` `blocked`\n- Decision Log\n  - Append-only schema `Q1..Q3` `done`\n  - Outcome auto-pairing `Q2..Q4` `live`\n  - Auditor evidence pack `Q3..Q4`\n- Adoption\n  - Pilot onboarding `Q1..Q2` `done`\n  - Org-wide enablement `Q2..Q4` `at-risk`\n  - Per-decision profiles `Q3..Q4`\n"
}
