---
marp: true
theme: indaco
size: hd
paginate: true
header: "Lattice · universal token system"
footer: "Phase 5 — sequential ramp"
---

<!-- _class: title -->
<!-- _paginate: false -->
<!-- _header: '' -->
<!-- _footer: '' -->

# One word, one meaning.

`Universal token system · Phase 5`

*The ordered colour ramp stops borrowing the name of the type multiplier — `--scale-*` becomes `--seq-*`.*

---

<!-- _class: cards-grid -->

## "scale" meant two unrelated things.

- The collision
  - `--scale-500` was an ordered colour ramp; `--fs-scale` was the type-size multiplier. Same word, unrelated jobs.
- seq
  - The sequential ramp is now `--seq-50` … `--seq-900` — ordered/quantitative encoding (heat, intensity).
- fs-scale
  - Untouched, and now unambiguous: the only "scale" left is the typographic one.
- Read it right
  - `--seq-700` is a ramp stop; it can never be mistaken for a font size again.

---

<!-- _class: word-cloud spectrum -->

## The seq ramp in use — frequency as heat.

- execution `5`
- discipline `4`
- velocity `4`
- talent `3`
- cadence `2`
- risk `1`

---

<!-- _class: divider -->

## Sequential is for ordered data.

`Not categorical`

The `--seq-*` ramp encodes magnitude — heat, progress intensity, faint grid. The unordered groups stay on the categorical `--cat-*` cycle from phase 1.

---

<!-- _class: closing -->
<!-- _paginate: false -->
<!-- _header: '' -->
<!-- _footer: '' -->

## No more two-meaning "scale".

`Phase 5 of 7 · see engineering/decisions/2026-06-11-universal-token-system.md`
