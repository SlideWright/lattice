/* Auto-generated by tools/build-exemplar-core.js — DO NOT EDIT.
   Source: lib/exemplars/tier-filter.js. Rebuild: npm run exemplar-core:build */
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// lib/exemplars/tier-filter.js
var require_tier_filter = __commonJS({
  "lib/exemplars/tier-filter.js"(exports, module) {
    "use strict";
    var TIER_ORDER = { short: 0, standard: 1, full: 2 };
    var TIERS2 = ["short", "standard", "full"];
    var DEFAULT_TIER2 = "standard";
    var TIER_RE = /<!--\s*tier:\s*(short|standard|full)\s*-->/i;
    function isTier(name) {
      return Object.prototype.hasOwnProperty.call(TIER_ORDER, name);
    }
    function splitDeck2(source) {
      const text = String(source == null ? "" : source).replace(/\r\n/g, "\n");
      let frontmatter = "";
      let body = text;
      if (/^---\n/.test(text)) {
        const end = text.indexOf("\n---", 4);
        if (end !== -1) {
          const after = end + "\n---".length;
          if (text[after] === "\n" || after === text.length) {
            frontmatter = text.slice(0, after);
            body = text.slice(after + 1);
          }
        }
      }
      const slides = body.trim().length ? body.split(/\n-{3}\n/) : [];
      return { frontmatter, slides };
    }
    function slideTier2(slide) {
      const m = String(slide).match(TIER_RE);
      return m ? m[1].toLowerCase() : DEFAULT_TIER2;
    }
    function filterToTier2(source, tier) {
      const want = isTier(tier) ? tier : DEFAULT_TIER2;
      const wantRank = TIER_ORDER[want];
      const { frontmatter, slides } = splitDeck2(source);
      const kept = slides.filter((slide) => TIER_ORDER[slideTier2(slide)] <= wantRank).map(
        (slide) => (
          // Remove the marker (and a stray blank line it may leave behind).
          slide.replace(new RegExp(`${TIER_RE.source}\\n?`, "i"), "").replace(/\n{3,}/g, "\n\n")
        )
      );
      const bodyOut = kept.join("\n\n---\n\n");
      if (!frontmatter) return `${bodyOut}
`;
      return bodyOut ? `${frontmatter}

${bodyOut}
` : `${frontmatter}
`;
    }
    function tierCounts2(source) {
      const { slides } = splitDeck2(source);
      const ranks = slides.map((s) => TIER_ORDER[slideTier2(s)]);
      return {
        short: ranks.filter((r) => r <= TIER_ORDER.short).length,
        standard: ranks.filter((r) => r <= TIER_ORDER.standard).length,
        full: ranks.filter((r) => r <= TIER_ORDER.full).length
      };
    }
    module.exports = { filterToTier: filterToTier2, tierCounts: tierCounts2, splitDeck: splitDeck2, slideTier: slideTier2, TIERS: TIERS2, DEFAULT_TIER: DEFAULT_TIER2 };
  }
});

// lib/exemplars/exemplar-core.entry.js
var import_tier_filter = __toESM(require_tier_filter());
var { filterToTier, tierCounts, splitDeck, slideTier, TIERS, DEFAULT_TIER } = import_tier_filter.default;
export {
  DEFAULT_TIER,
  TIERS,
  filterToTier,
  slideTier,
  splitDeck,
  tierCounts
};
