/* Auto-generated by tools/build-standalone-core.js — DO NOT EDIT.
   Source: lib/components/chart/_chart-family/standalone-svg.js. Rebuild: npm run standalone-core:build */
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// lib/components/chart/_chart-family/standalone-svg.js
var require_standalone_svg = __commonJS({
  "lib/components/chart/_chart-family/standalone-svg.js"(exports, module) {
    var STYLE_PROPS2 = [
      "fill",
      "fill-opacity",
      "fill-rule",
      "stroke",
      "stroke-width",
      "stroke-opacity",
      "stroke-linecap",
      "stroke-linejoin",
      "stroke-dasharray",
      "stroke-dashoffset",
      "stroke-miterlimit",
      "opacity",
      "color",
      // Gradient stops carry their colour here, NOT in `fill` — and the chart's
      // swatch/wedge/spine gradients reference var()/color-mix() in stop-color, so
      // these must be captured or the gradients render black when detached.
      "stop-color",
      "stop-opacity",
      "font-family",
      "font-size",
      "font-weight",
      "font-style",
      "font-variant",
      "letter-spacing",
      "word-spacing",
      "text-anchor",
      "dominant-baseline",
      "alignment-baseline",
      "paint-order",
      "mix-blend-mode"
    ];
    var INITIAL2 = {
      "fill-opacity": "1",
      "fill-rule": "nonzero",
      "stroke": "none",
      "stroke-opacity": "1",
      "stroke-width": "1px",
      "stroke-linecap": "butt",
      "stroke-linejoin": "miter",
      "stroke-dasharray": "none",
      "stroke-dashoffset": "0px",
      "stroke-miterlimit": "4",
      "opacity": "1",
      "font-style": "normal",
      "font-variant": "normal",
      "font-weight": "400",
      "letter-spacing": "normal",
      "word-spacing": "0px",
      "dominant-baseline": "auto",
      "alignment-baseline": "auto",
      "paint-order": "normal",
      "mix-blend-mode": "normal"
    };
    var SVG_NS2 = "http://www.w3.org/2000/svg";
    function flattenSvgStyles2(srcSvg, win) {
      const w = win || (typeof window !== "undefined" ? window : null);
      if (!w) throw new Error("flattenSvgStyles requires a browser window");
      const PROPS = [
        "fill",
        "fill-opacity",
        "fill-rule",
        "stroke",
        "stroke-width",
        "stroke-opacity",
        "stroke-linecap",
        "stroke-linejoin",
        "stroke-dasharray",
        "stroke-dashoffset",
        "stroke-miterlimit",
        "opacity",
        "color",
        "stop-color",
        "stop-opacity",
        "font-family",
        "font-size",
        "font-weight",
        "font-style",
        "font-variant",
        "letter-spacing",
        "word-spacing",
        "text-anchor",
        "dominant-baseline",
        "alignment-baseline",
        "paint-order",
        "mix-blend-mode"
      ];
      const INIT = {
        "fill-opacity": "1",
        "fill-rule": "nonzero",
        "stroke": "none",
        "stroke-opacity": "1",
        "stroke-width": "1px",
        "stroke-linecap": "butt",
        "stroke-linejoin": "miter",
        "stroke-dasharray": "none",
        "stroke-dashoffset": "0px",
        "stroke-miterlimit": "4",
        "opacity": "1",
        "font-style": "normal",
        "font-variant": "normal",
        "font-weight": "400",
        "letter-spacing": "normal",
        "word-spacing": "0px",
        "dominant-baseline": "auto",
        "alignment-baseline": "auto",
        "paint-order": "normal",
        "mix-blend-mode": "normal"
      };
      const doc = srcSvg.ownerDocument;
      const SVGNS = "http://www.w3.org/2000/svg";
      const probe = doc.createElementNS(SVGNS, "rect");
      probe.setAttribute("width", "0");
      probe.setAttribute("height", "0");
      (srcSvg.parentNode || srcSvg).appendChild(probe);
      function resolveColor(expr) {
        if (!expr) return "";
        probe.style.color = "";
        probe.style.color = expr;
        return w.getComputedStyle(probe).color || expr;
      }
      function walk(src) {
        const clone = src.cloneNode(false);
        if (src.nodeType === 1) {
          const cs = w.getComputedStyle(src);
          const prev = src.getAttribute ? src.getAttribute("style") : null;
          const tag = src.tagName ? src.tagName.toLowerCase() : "";
          let decl = "";
          if (tag === "stop") {
            const expr = src.style.stopColor || src.getAttribute("stop-color") || cs.getPropertyValue("stop-color");
            const col = resolveColor(expr);
            if (col) decl += "stop-color:" + col + ";";
            const so = src.style.stopOpacity || src.getAttribute("stop-opacity");
            if (so) decl += "stop-opacity:" + so + ";";
          } else {
            for (let i = 0; i < PROPS.length; i++) {
              const p = PROPS[i];
              const v = cs.getPropertyValue(p);
              if (!v) continue;
              if (Object.hasOwn(INIT, p) && v === INIT[p]) continue;
              decl += p + ":" + v + ";";
            }
          }
          const keepPrev = tag === "stop" ? "" : prev || "";
          if (decl || keepPrev) clone.setAttribute("style", decl + keepPrev);
          for (let n = src.firstChild; n; n = n.nextSibling) {
            if (n === probe) continue;
            if (n.nodeType === 1 || n.nodeType === 3) clone.appendChild(walk(n));
          }
        }
        return clone;
      }
      const out = walk(srcSvg);
      probe.remove();
      if (out.setAttribute && !out.getAttribute("xmlns")) out.setAttribute("xmlns", "http://www.w3.org/2000/svg");
      return out;
    }
    function collectFontFamilies2(svgMarkup) {
      if (!svgMarkup) return [];
      const text = String(svgMarkup).replace(/&quot;|&#34;|&apos;|&#39;/g, "").replace(/&amp;/g, "&");
      const seen = /* @__PURE__ */ new Map();
      const re = /font-family\s*[:=]\s*"?([^;">]+)/gi;
      let m;
      while (m = re.exec(text)) {
        for (const raw of m[1].split(",")) {
          const fam = raw.trim().replace(/['"]/g, "").trim();
          if (!fam) continue;
          const key = fam.toLowerCase();
          if (!seen.has(key)) seen.set(key, fam);
        }
      }
      return Array.from(seen.values());
    }
    function finalizeStandaloneSvg2(svgMarkup, opts) {
      const o = opts || {};
      const src = String(svgMarkup || "").trim();
      const open = src.match(/^<svg\b([^>]*?)\/?>/i);
      if (!open) throw new Error("finalizeStandaloneSvg: input is not an <svg> element");
      let attrs = open[1];
      if (!/\bxmlns\s*=/.test(attrs)) attrs += ` xmlns="${SVG_NS2}"`;
      if (/\bxlink:/.test(src) && !/\bxmlns:xlink\s*=/.test(attrs)) {
        attrs += ' xmlns:xlink="http://www.w3.org/1999/xlink"';
      }
      if (!/\bwidth\s*=/.test(attrs) || !/\bheight\s*=/.test(attrs)) {
        const vb = attrs.match(/\bviewBox\s*=\s*"(\s*[-\d.]+\s+[-\d.]+\s+([-\d.]+)\s+([-\d.]+)\s*)"/i);
        if (vb) {
          if (!/\bwidth\s*=/.test(attrs)) attrs += ` width="${vb[2]}"`;
          if (!/\bheight\s*=/.test(attrs)) attrs += ` height="${vb[3]}"`;
        }
      }
      const body = src.slice(open[0].length);
      const fontCss = (o.fontFaceCss || "").trim().replace(/]]>/g, "]]]]><![CDATA[>");
      const styleBlock = fontCss ? `<defs><style type="text/css"><![CDATA[
${fontCss}
]]></style></defs>` : "";
      const prolog = o.xmlProlog === false ? "" : '<?xml version="1.0" encoding="UTF-8"?>\n';
      return `${prolog}<svg${attrs}>${styleBlock}${body}`;
    }
    module.exports = {
      STYLE_PROPS: STYLE_PROPS2,
      INITIAL: INITIAL2,
      SVG_NS: SVG_NS2,
      flattenSvgStyles: flattenSvgStyles2,
      collectFontFamilies: collectFontFamilies2,
      finalizeStandaloneSvg: finalizeStandaloneSvg2
    };
  }
});

// lib/components/chart/_chart-family/standalone-core.entry.js
var import_standalone_svg = __toESM(require_standalone_svg());
var {
  STYLE_PROPS,
  INITIAL,
  SVG_NS,
  flattenSvgStyles,
  collectFontFamilies,
  finalizeStandaloneSvg
} = import_standalone_svg.default;
export {
  INITIAL,
  STYLE_PROPS,
  SVG_NS,
  collectFontFamilies,
  finalizeStandaloneSvg,
  flattenSvgStyles
};
