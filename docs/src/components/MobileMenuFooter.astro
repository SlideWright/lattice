---
// Override of Starlight's MobileMenuFooter — intentionally empty.
//
// The default renders a second theme picker (and language/social) at the
// bottom of the mobile menu. Our single light/dark + palette controls live in
// the header (which stays fixed above the open menu), and GitHub is in the
// .lx-sitenav group, so a duplicate theme dropdown here would just be
// confusing. Rendering nothing keeps one control surface for the whole site.
---
