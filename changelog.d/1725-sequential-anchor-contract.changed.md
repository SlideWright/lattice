- The sequential ramp's anchor `--seq-500` is now part of the per-theme token contract. A generated theme derives it (`deriveTheme` emits the brand accent's light arm and that same hue and chroma at OKLab L 0.68 for the dark arm) instead of falling through to the engine's `var(--accent)` default, which is near-white on a dark canvas and left the derived stops 1.36–1.42:1 apart. Measured across eight generated palettes: the tightest adjacent step on a dark canvas goes 0.082–0.096 → 0.120–0.141 OKLab, with every stop still clearing its 3:1 bar. Light-canvas output is unchanged.
- `token-parity` and `scorecard:check` now hold every committed palette to `--seq-500`; all fourteen already declared one, so no palette value moves.
- `npm run new:theme`'s author checklist gained the ramp anchor (item 9): the scaffold's light arm tracks your brand by reference, the dark arm is still indaco's blue and has to be re-anchored at OKLab L 0.68 in your own hue.
