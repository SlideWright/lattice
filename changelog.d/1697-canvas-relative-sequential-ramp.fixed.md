- **The sequential ramp (`--seq-*`) follows the canvas.** Stops 600–900 used to shade toward black on every canvas, so on a dark one they walked *toward* the background — `word-cloud spectrum`'s weight-4 words sat at 1.66–3.19:1 on eleven palettes. The ramp now interpolates toward `--seq-pole-low` / `--seq-pole-high`, a `light-dark()` pair, so a higher stop is always the louder one whichever canvas it lands on. Light-canvas values are unchanged.
- **Every palette's dark `--seq-500` anchor is re-curated** to a mid-range value (OKLab L 0.68, hue and chroma untouched; thirteen dark arms across the fourteen palettes that declare one). Restating the near-white dark `--accent` left no headroom above it, which would have traded an illegible top stop for one indistinguishable from the tier next to it. `mustard`'s light anchor moves too (`#8C6A18` → `#7B5A00`) — its low stops receded past its own cream canvas toward pure white, the one sub-3:1 stop on a light canvas.
- **`word-cloud spectrum`'s heaviest tier reads `--seq-900` instead of `var(--accent)`.** A brand hue is not a stop on a sequential scale: its lightness is whatever the palette needed for an accent, so weight 4 out-shouted weight 5 on 20 of 38 palette × canvas combinations, and where a palette anchored the ramp on `--accent`, weights 5 and 3 were the same color. All four loud tiers are ramp stops now and the encoding is ordered by construction; the quiet floor stays `--text-muted`.
- On the engine path (Studio, the docs Playground), where a palette's curated anchor wins, tiers below their 3:1 bar go 13 → 0, the worst tier-vs-canvas 1.66:1 → 3.17:1, the tightest adjacent step 0.012 → 0.096 in OKLab, and 38 of 38 encodings are strictly monotonic (was 7). PDF export used the base's `var(--accent)` anchor until #1527's concat flip, which lands in this same release: on its own this change took export to sub-3:1 tiers 13 → 3 and strict monotonicity 0/38 → 25/38, and the flip carries it the rest of the way to 0 and 38/38, closing `mustard`'s remaining 2.86:1 word fill and the twelve combinations that painted their three loudest tiers in one color. No palette's tightest adjacent step regresses on either path.
